# Example env file. Do NOT put real private keys in source control.
# Use a 64-character hex private key (no 0x prefix) or prefix with 0x.
# Locally you can run: VERIFIER_PRIVATE_KEY=<your_key> node server.js
VERIFIER_PRIVATE_KEY=YOUR_PRIVATE_KEY_HERE